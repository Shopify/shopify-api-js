import { Session } from '../auth/session';
/**
 * Stores the current user's session.
 *
 * @param Session Session object
 */
export default function storeSession(session: Session): Promise<boolean>;
//# sourceMappingURL=store-session.d.ts.map