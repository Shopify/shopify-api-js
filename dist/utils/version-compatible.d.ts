import { ApiVersion } from '../base_types';
/**
 * Check if the current or optionally supplied version is compatible with a given version
 */
export default function versionCompatible(referenceVersion: ApiVersion, currentVersion?: ApiVersion): boolean;
//# sourceMappingURL=version-compatible.d.ts.map