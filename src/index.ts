import {Context, ContextParams} from './context';
import {ShopifyError} from './error';

const Shopify = {
  ShopifyError,
};

export default Shopify;
export {
  Context,
  ContextParams,
  ShopifyError,
};
